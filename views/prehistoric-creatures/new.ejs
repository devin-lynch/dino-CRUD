<h2>Make a new Prehistoric Creature:</h2>

<!-- 
    POST vs GET form
    GET forms put the data into the query string of the url -- in express you use the req.query (used for READ queries)
    POST forms put the data in the request body -- and in express we access it through the req.body (used for CREATE queries)
 -->

<form method="POST" action="/prehistoric_creatures">
    <label for="creatureType">Type:</label>
    <input id="creatureType" type="text" name="type" />

    <label for="creatureImg">Image:</label>
    <input id="creatureImg" type="text" name="img_url" />

    <button type-="submit">Create Prehistoric Creature</button>
</form>